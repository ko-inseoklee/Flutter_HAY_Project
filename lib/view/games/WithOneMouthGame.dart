import 'package:flutter/cupertino.dart';

class WithOneMouthGame extends StatelessWidget {
  const WithOneMouthGame({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
